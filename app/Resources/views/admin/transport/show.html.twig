{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_transport_show' %}

{% block main %}
    <h1>{{ transport.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'label.summary'|trans }}</th>
                <td>{{ transport.name|md2html }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}

{% block sidebar %}
    <div class="section">
        <a href="{{ path('admin_transport_edit', { id: transport.id }) }}" class="btn btn-lg btn-block btn-success">
            <i class="fa fa-edit"></i> {{ 'action.edit_contents'|trans }}
        </a>
    </div>

    <div class="section">
        {{ include('admin/transport/_form.html.twig', {
            form: delete_form,
            button_label: 'action.delete_transport'|trans,
            button_css: 'btn btn-lg btn-block btn-danger',
            show_confirmation: true,
        }, with_context = false) }}
    </div>

    {{ show_source_code(_self) }}
{% endblock %}
