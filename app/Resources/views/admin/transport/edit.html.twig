{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin_transport_edit' %}

{% block main %}
    <h1>{{ 'title.edit_transport'|trans({'%id%': transport.id}) }}</h1>

    {{ include('admin/transport/_form.html.twig', {
        form: edit_form,
        button_label: 'action.save'|trans,
        include_back_to_home_link: true,
    }, with_context = false) }}
{% endblock %}

{% block sidebar %}
    <div class="section actions">
        {{ include('admin/transport/_form.html.twig', {
            form: delete_form,
            button_label: 'action.delete_transport'|trans,
            button_css: 'btn btn-lg btn-block btn-danger',
            show_confirmation: true,
        }, with_context = false) }}
    </div>

    {{ parent() }}

    {{ show_source_code(_self) }}
{% endblock %}
